# OpenAI Agents SDK + Ollama (Chat Completions compatible)
openai-agents>=0.0.1
openai>=1.0.0

# Jinja2 for template rendering
Jinja2>=3.1.0

# Optional: LiteLLM if you want to use litellm/ollama/... later
# pip install "openai-agents[litellm]"
